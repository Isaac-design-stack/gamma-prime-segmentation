# Data and Code Availability

The data and code supporting the findings of this study, including the γ′ segmentation micrographs and annotations, fixed train/validation splits, trained-model artefacts, evaluation outputs, threshold-selection results, deployment metrics, figures, and executable analysis code, are archived in Zenodo at **[INSERT DOI]**. The source code is also mirrored at **[INSERT GITHUB URL]**. The archived release contains the files required to reproduce the reported segmentation workflow and derived results.

The public release contains 110 merged micrographs, the corresponding one-class COCO annotation set (30,305 γ′ instances), and the exact 88/22 train-validation split used in the archived experiment. The large trained checkpoint `model_best_ap50.pth` should be included in the Zenodo release; `model_final.pth` may also be included as an additional training artefact.

Before publication, the authors should confirm that all source micrographs may legally be redistributed and select licences appropriate to the code, data, annotations, and model weights.
